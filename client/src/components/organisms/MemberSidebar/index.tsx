import React from "react";
import * as s from "./styles";

type Props = {};

const MemberSidebar = (props: Props) => {
  return <s.MembersContainer>MemberSidebar</s.MembersContainer>;
};

export default MemberSidebar;
